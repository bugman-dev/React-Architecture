import PublicPage from '../../pages/publicPage';

/**
 * List of all public components and routes.
 * Append new public page in the array.
 */
export const publicRoute = [
  {
    path: '/',
    element: PublicPage
  }
];
